---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name }}-mongo-secret"
type: Opaque
data:
  username: {{ .Values.mongodb.secret.username | quote }}
  password: {{ .Values.mongodb.secret.password | quote }}
