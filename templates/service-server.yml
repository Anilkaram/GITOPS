apiVersion: v1
kind: Service
metadata:
  name: server-service
spec:
  selector:
    app: {{ .Values.server.labels.app }}
  type : {{ .Values.server.type}}
  ports:
    - port: {{ .Values.server.ports.port }}
      targetPort: {{ .Values.server.ports.targetPort }}
 
